services:
  app:
    image: python:3.12-slim
    working_dir: /app
    volumes:
      - .:/app
    ports:
      - "8000:8000"
    command: >
      sh -c "pip install -e . &&
      uvicorn src.api.server:app --host 0.0.0.0 --port 8000"
